{% if (form) %}
<div class="c-form">
    <h3 class="c-form__heading">{{  translate(formLabel or 'Comment') }}</h3>
    {{ form(form) }}
    <div class="c-form__content">
        {{ content|raw }}
    </div>
</div>
{% endif %}
