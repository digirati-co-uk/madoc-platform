{% if canvasEncode %}
    <li>
        <div class="c-action c-flagged c-action--link">
            <a href="{{ url('site/elucidate_canvas_activity', { canvas: canvasEncode }, true) }}"
               class="link--plain" aria-label="{{ translate('View activity') }}">
                <span class="fa fa-bolt" aria-hidden="true"></span> {{ translate('View activity') }}
            </a>
        </div>
    </li>
{% endif %}
