
{% do form.setAttribute('action', url('admin/model-import/process', {'action': 'process'})) %}
{%  do form.prepare() %}

{{ pageTitle(translate('Import A Capture Model from Json or Yaml')) }}

{{ form().openTag(form) | raw }}

<fieldset id='page-actions'>
    <button type="submit">{{ translate('Next') }}</button>
</fieldset>

{{ formCollection(form, false) }}

{{ form().closeTag(form) | raw }}
