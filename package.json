{
  "version": "1.0.0",
  "scripts": {
    "restart:capture-models": "bin/madoc test-compose up -d --no-deps --build model-api",
    "restart:tasks": "bin/madoc test-compose up -d --no-deps --build tasks-api",
    "restart:queue": "docker-compose exec madoc-ts pm2 restart queue"
  },
  "devDependencies": {
    "cypress-intellij-reporter": "^0.0.4"
  }
}
