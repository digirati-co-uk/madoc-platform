---
- hosts: "{{instance}}"
  vars_files:
    - vars/compose.yml
  roles:
    - docker-compose

  tasks:
    - command: "cat /srv/madoc/docker-compose.yml"
      register: cat
    - debug:
        var: cat
