import React from 'react';
import StandardButton from '../StandardButton';

export default (
  <StandardButton size="medium" variation="primary" uxpId="StandardButton-default">
    Default button
  </StandardButton>
);
