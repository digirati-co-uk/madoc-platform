import React from 'react';
import StandardButton from '../StandardButton';

export default (
  <StandardButton size="large" variation="primary" uxpId="StandardButton-lg-pr">
    Default button
  </StandardButton>
);
