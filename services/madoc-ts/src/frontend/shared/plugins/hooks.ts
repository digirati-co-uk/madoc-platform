import { useApi } from '../hooks/use-api';
import { useAtoms } from './use-atoms';
import { useComponents } from './use-components';
import { useModule } from './use-module';

export const hooks = {
  useAtoms,
  useModule,
  useApi,
  useComponents,
};
