import { MadocTheme } from './definitions/types';

export const defaultTheme: MadocTheme = {
  fonts: 'default',
  header: 'default',
  accent: 'default',
  siteContainer: 'default',
  footer: 'light',
  global: 'default',
};
