import { CastANetOverlayAtlasProps } from './CastANetOverlayAtlas';

export function CastANetReadonly({
  value,
  onChange,
  disabled,
  activeCell,
  overlayColors,
  dimOpacity = 0,
  previewOverlayOnly,
  onOverlayWheel,
}: CastANetOverlayAtlasProps) {
  console.log(value);
  return null;
}
