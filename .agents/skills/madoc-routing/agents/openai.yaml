interface:
  display_name: "Madoc Routing"
  short_description: "HTTP routes, permissions, activity streams, and IIIF"
  default_prompt: "Use $madoc-routing to trace or change a Madoc HTTP endpoint."
